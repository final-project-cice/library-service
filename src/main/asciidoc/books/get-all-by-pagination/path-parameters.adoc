.+http://localhost:8082/books/{startPage}/{pageSize}+
|===
|Parameter|Description

|`+startPage+`
|zero-based page index, must not be negative

|`+pageSize+`
|the size of the page to be returned, must be greater than 0

|===