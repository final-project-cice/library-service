#============================== Configuration Apache TomCat
server:
  port: 5000
#============================== Configuration Logging
logging:
  file: /tmp/log/library-service_test.log
  level:
    root: INFO
    org.springframework: INFO
    org.springframework.data: INFO
    guru.springframework.controllers: INFO
    org.hibernate: INFO
    com.trl.libraryservice: DEBUG

spring:
  profiles.active: test
  application:
    name: library-service

  #============================== Configuration PostgreSQL
  datasource:
    username: developer
    password: Nqa2GrwJ5dqu
    url: jdbc:postgresql://ec2-15-188-76-238.eu-west-3.compute.amazonaws.com/library_service_test
    driver-class-name: org.postgresql.Driver

  #============================== Configuration JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create